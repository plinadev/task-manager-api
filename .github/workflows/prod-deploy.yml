name: Prod Deploy

on:
  workflow_dispatch:

jobs:
  deploy-prod:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      env-name: production
      db-env-name: db
      github-env: prod
    secrets: inherit
