name: Dev Deploy

on:
  push:
    branches: [dev]
  workflow_dispatch:

jobs:
  deploy-dev:
    uses: ./.github/workflows/build-and-deploy.yml
    with:
      env-name: self-hosted dev
      github-env: dev
